services:
  test-viajero-api:
    build: ./backend
    ports:
      - "8082:8080"
    volumes:
      - viajero_data:/data
    env_file:
      - ./backend/.env
    networks:
      - web
    restart: unless-stopped

volumes:
  viajero_data:

networks:
  web:
    external: true